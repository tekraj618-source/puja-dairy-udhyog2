document.addEventListener("DOMContentLoaded",()=>{document.querySelectorAll("nav a").forEach(a=>a.onclick=()=>document.body.classList.remove("nav-open"));const f=document.getElementById("orderForm");if(!f)return;const qs=[...document.querySelectorAll(".qty")],s=document.getElementById("summary"),g=document.getElementById("grandTotal"),i=document.getElementById("items"),t=document.getElementById("total");function u(){let total=0,lines=[];qs.forEach(q=>{let n=Math.max(0,Number(q.value||0)),p=Number(q.dataset.price||0);if(n){let x=n*p;total+=x;lines.push(`${q.dataset.name} x ${n} = Rs. ${x.toFixed(0)}`)}});s.innerHTML=lines.length?lines.map(x=>`<div class="summary-line">${x}</div>`).join(""):'<p class="muted">Add products to see your summary.</p>';g.textContent=`Rs. ${total.toFixed(0)}`;i.value=lines.join(" | ");t.value=total.toFixed(2)}qs.forEach(q=>q.addEventListener("input",u));f.addEventListener("submit",u)});